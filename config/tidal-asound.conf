# ALSA configuration for Tidal Connect
# Routes audio output to FIFO for Snapcast integration
#
# Audio format: 44100:16:2 (CD quality, stereo)
# Matches snapMULTI's standard format across all sources.

pcm.!default {
    type plug
    slave {
        pcm snapfifo
        format S16_LE
        rate 44100
        channels 2
    }
}

pcm.snapfifo {
    type file
    slave.pcm null
    file "/audio/tidal_fifo"
    format "raw"
}
