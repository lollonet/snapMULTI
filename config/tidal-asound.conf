# ALSA configuration for Tidal Connect
# Routes audio output to FIFO for Snapcast integration
#
# Audio format: 44100:16:2 (CD quality, stereo)
# This config routes tidal-connect audio to the FIFO at snapMULTI's
# standard audio format used across all sources.

pcm.!default {
    type plug
    slave.pcm snapfifo
}

pcm.snapfifo {
    type file
    slave.pcm null
    file "/audio/tidal_fifo"
    format "raw"
}
