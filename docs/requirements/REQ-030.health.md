---
id: REQ-030
domain: requirements
status: approved
source_of_truth: false
related: [ARC-002, TECH-001]
acceptance:
  - "Given all containers are running, when healthcheck runs, then each service reports healthy within start_period."
  - "Given a service crashes, when Docker detects unhealthy state, then the container restarts automatically."
derived_from: [VISION-003]
---

# REQ-030: Container Health Monitoring

## Description

Every container has a Docker healthcheck that verifies the service is responsive. Docker's restart policy (`unless-stopped`) ensures automatic recovery.

## Healthchecks

| Service | Method | Interval | Start Period |
|---------|--------|----------|-------------|
| snapserver | `wget :1780/` | 30s | 15s |
| shairport-sync | `pgrep + test FIFO` | 30s | 10s |
| librespot | `pgrep + wget API` | 30s | 15s |
| mpd | `nc 127.0.0.1 6600` | 30s | 60s (300s NFS) |
| mympd | `wget :8180/` | 30s | 60s (300s NFS) |
| metadata | `urllib :8083/health` | 30s | 10s |
| tidal-connect | `pgrep tidal` | 30s | 10s |

## Recovery

- `restart: unless-stopped` on all containers
- 3 retries before marking unhealthy
- 10s timeout per healthcheck probe
- Service dependency chain: snapserver must be healthy before sources start

## Dependencies

- Docker Engine healthcheck support
